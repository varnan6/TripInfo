services:
  - type: web
    name: trip-info-api
    runtime: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/trip-info-api-1.0.0.jar
    envVars:
      - key: PORT
        value: 8080
